spring:
  profiles.active: ${SPRING_PROFILES_ACTIVE:dev}
  application.name: Rookie-Blog
  config:
    import: optional:file:.env[.properties]
  graphql:
    cors:
      allowed-origins: ${GRAPHQL_CORS_ALLOWED_ORIGINS:localhost:5173}
server:
  port: ${SERVER_PORT:8080}
  servlet:
    context-path: /api
project:
  github:
    repo: https://github.com/patton174/Rookie-Blog/tree/backend
